#!/bin/sh
# Strip Cursor agent co-author trailers so GitHub does not list cursoragent as a contributor.
grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
